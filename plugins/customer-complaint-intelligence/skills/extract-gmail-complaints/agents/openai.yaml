interface:
  display_name: "Extract Gmail Complaints"
  short_description: "Turn complaint threads into a source-linked CSV"
  default_prompt: "Extract the customer complaints in this Gmail scope into a source-linked CSV."
policy:
  allow_implicit_invocation: true
